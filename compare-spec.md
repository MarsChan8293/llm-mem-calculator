# 图表Spec（绘图规范说明书）
## 1. 图表基础信息
**图表名称：Models Smaller than 500B**
用途：对比5款500B参数量以内大模型，在不同上下文长度下的KV Cache显存占用（单位GB），折线图类型。

## 2. 坐标轴定义
### X轴（横轴）
- 名称：`Sequence Length`（序列长度）
- 值域：`0 ~ 1,000,000（1M）`
- 刻度点位：`0、200K、400K、600K、800K、1M`，刻度文本直接带K/M单位
- 轴位置：图表底部

### Y轴（纵轴）
- 名称：`Cache Memory (GB)`（缓存占用显存，GB）
- 值域：`0 ~ 330GB`
- 刻度点位：`0、50、100、150、200、250、300`
- 轴位置：图表左侧

## 3. 图例（Legend）
- 摆放位置：**左上角内嵌图表区域**
- 条目与配色、曲线对应规则：
| 线条颜色 | 模型名称 |
| ---- | ---- |
| 浅粉紫色 | Hunyuan-3 |
| 暗红色 | MiniMax-M2 |
| 草绿色 | Qwen-3.5 |
| 橘橙色 | MiMo-V2.5 |
| 藏青蓝色 | DeepSeek-V4-Flash |
- 样式：白色填充图例框，黑色细边框。

## 4. 曲线数据&趋势（全部为线性直线，原点(0,0)起始）
> 所有曲线起点统一在 `Sequence Length=0，Cache=0GB`
1. **Hunyuan-3（粉紫）**：斜率最大，Seq=1M时，Cache≈322GB（全图最高）
2. **MiniMax-M2（暗红）**：次高斜率，Seq=1M时，Cache≈250GB
3. **Qwen-3.5（绿色）**：低斜率平缓上升，Seq=1M时，Cache≈32GB
4. **MiMo-V2.5（橙色）**：略低于Qwen，Seq=1M时，Cache≈23GB
5. **DeepSeek-V4-Flash（深蓝）**：全图最低，紧贴X轴，Seq=1M时，Cache≈5GB

## 5. 画布&网格样式规范
1. 画布底色：纯白色
2. 网格线：浅灰色、短虚线，横向纵向全网格；网格透明度低，不遮挡折线
3. 边框：仅保留左、下坐标轴实线边框；上、右侧图表外框隐藏
4. 折线样式：纯色实线，无圆点标记、无阴影、无填充

## 6. 排版文字规范
1. 标题：图表顶部居中，加粗常规字体
2. X/Y轴标签：坐标轴外侧居中放置，常规字号
3. 刻度文字：坐标轴旁，常规无衬线字体

## 7. 核心业务结论备注（图表表达含义）
传统大模型（Hunyuan-3、MiniMax-M2）KV Cache显存随上下文长度线性暴涨；搭载KV缓存优化技术的Qwen-3.5、MiMo-V2.5、DeepSeek-V4-Flash可极大压缩长序列显存占用，其中DeepSeek-V4-Flash缓存优化效果最优。

需要替换模型时：仅修改【图例条目、对应颜色、1M处终点GB数值】，曲线仍保持从原点出发的直线逻辑。